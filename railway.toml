[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["jdk21"]

[deploy]
startCommand = "java -Xmx300m -Xms128m -jar target/khatha-backend-2.2.6.jar"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
