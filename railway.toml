[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -Xmx256m -Xms64m -XX:+UseContainerSupport -jar target/khatha-backend-2.2.6.jar"
healthcheckPath = "/api/public/ping"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
